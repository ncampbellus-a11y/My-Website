kind = "web"
previewPath = "/"
title = "ClarevAI"
version = "1.0.0"
id = "artifacts/clarevai"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24937

[services.development]
run = "pnpm --filter @workspace/clarevai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/clarevai", "run", "build" ]
publicDir = "artifacts/clarevai/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24937"
BASE_PATH = "/"
